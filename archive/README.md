# Archived workflow sources

This directory preserves workflow sources that are no longer part of the active catalog or new installations.

`post-this-next` is excluded from this branch's active distribution while the experimental Reply First replacement is reviewed. Its published release artifacts remain immutable, and the legacy `post-this-next` CLI dispatch remains available for compatibility. Files below `archive/` are intentionally excluded from release bundles.
