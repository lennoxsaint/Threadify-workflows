{
  "workflow_id": "daily-greatest-hits",
  "version": "0.1.0",
  "title": "Daily Greatest Hits",
  "summary": "Use a user-approved greatest-hits source pack or basic greatest-hits readback, prepare daily post candidates and optional proof/use-case lanes, validate approved copy, schedule only after explicit approval, and return receipts.",
  "kind": "recipe",
  "entrypoint": {
    "skill_id": null,
    "source": "workflows/daily-greatest-hits/README.md"
  },
  "bundle": { "kind": "recipe", "standalone": false },
  "dependencies": { "workflows": ["greatest-hits-runway"], "files": [] },
  "triggers": ["use my greatest hits for today's posts", "prepare daily candidates from a greatest-hits source pack"],
  "io": {
    "inputs": [{ "name": "greatest-hits source pack", "schema_ref": "inline:greatest-hits-source-pack" }],
    "outputs": [{ "name": "daily post candidates", "schema_ref": "schemas/threadify-ready-output.v1.json" }]
  },
  "supported_adapters": ["generic-mcp", "hermes", "gemini", "codex", "claude", "openclaw", "cursor"],
  "required_mcp_tools": [
    "get_connection_defaults",
    "greatest_hits",
    "validate_post",
    "schedule_post",
    "get_schedule_status",
    "list_scheduled_posts"
  ],
  "optional_mcp_tools": ["record_feedback"],
  "free_capabilities": [
    "connection defaults",
    "basic greatest-hits readback where exposed",
    "user-supplied greatest-hits source-pack intake",
    "optional public-safe proof/use-case lane review",
    "candidate packet review",
    "post validation",
    "approved scheduling",
    "schedule readback",
    "structured receipt capture"
  ],
  "paid_capability_stubs": [
    "native post generation",
    "native draft editing",
    "advanced analytics ranking",
    "Brain winner strategy",
    "hosted recurring automations",
    "native proof-lane generation",
    "premium workflow variants"
  ],
  "approval_gate": {
    "required": true,
    "type": "explicit-final-approval",
    "must_show": ["account handle", "source pack", "candidate text", "optional lane blockers", "scheduled times", "timezone", "action"]
  },
  "fallback": {
    "mode": "daily-greatest-hits-review-packet",
    "artifact_schema": "threadify-ready-output.v1",
    "instructions": [
      "Ask the user to provide top post links, approved source text, or a clean greatest-hits source pack when greatest_hits is unavailable.",
      "Return exact candidate texts and proposed schedule slots for manual review.",
      "Keep optional proof/use-case lane labels, receipts, blockers, CTA metadata, and media review state separate from public post copy.",
      "Do not claim Threadify generated, scheduled, or optimized posts if the connected tool was unavailable.",
      "Keep private metrics, account IDs, member data, and private proof paths out of the fallback artifact."
    ]
  },
  "receipt": {
    "schema": "workflow-receipt.v1",
    "must_prove": ["workflow ID", "adapter", "account handle", "source status", "approved text", "status", "timestamp", "tool path", "fallback state"]
  }
}
