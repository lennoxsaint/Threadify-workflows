{
  "name": "threadify-workflows",
  "version": "0.14.2",
  "description": "Local-first buyer-conversation workflows, agent discovery, and optional approved Threadify services.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "threadify-workflows": "./bin/threadify-workflows.mjs"
  },
  "scripts": {
    "bundle:build": "node scripts/build-qbr-bundle.mjs && node scripts/build-creator-bundles.mjs && node scripts/build-advanced-bundles.mjs && node scripts/build-workflow-bundles.mjs",
    "bundle:check": "node scripts/build-qbr-bundle.mjs --check && node scripts/build-creator-bundles.mjs --check && node scripts/build-advanced-bundles.mjs --check && node scripts/build-workflow-bundles.mjs --check",
    "release:build": "node scripts/build-release-assets.mjs",
    "test:installer": "node --test validation/installer-tests/installer.test.mjs validation/installer-tests/catalog.test.mjs",
    "validate": "node validation/schema-checks/validate.mjs && npm run bundle:check",
    "test:creator": "node --test validation/creator-tests/*.test.mjs",
    "test": "npm run validate && npm run test:installer && npm run test:creator && npm run test:offer-builder && npm run test:offer-decision && npm run test:registry && npm run test:conversations && npm run test:lead-desk && npm run test:niche && npm run test:profile",
    "test:offer-builder": "node --test validation/offer-builder-tests/*.test.mjs",
    "test:offer-decision": "node --test validation/offer-decision-tests/*.test.mjs",
    "test:registry": "node --test validation/registry-tests/*.test.mjs",
    "test:conversations": "node --test validation/conversation-tests/*.test.mjs",
    "test:niche": "node --test validation/niche-tests/*.test.mjs",
    "test:profile": "node --test validation/profile-tests/*.test.mjs",
    "test:lead-desk": "node --test validation/lead-desk-tests/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
